# 🚂 Railway.app 详细部署教程

**难度**: ⭐ 最简单  
**时间**: 10-15 分钟  
**费用**: 免费（每月 $5 额度）  
**推荐指数**: ⭐⭐⭐⭐⭐

---

## 📋 准备工作

### 1. 需要准备的账号
- [ ] GitHub 账号
- [ ] Railway 账号（可以用 GitHub 登录）

### 2. 需要准备的内容
- [ ] 项目代码已推送到 GitHub
- [ ] 确保代码中有 `package.json`
- [ ] 确保代码中有 `Dockerfile`（可选）

---

## 🎯 第一步：注册 Railway 账号

### 1.1 访问 Railway 官网
```
https://railway.app/
```

### 1.2 注册账号
1. 点击右上角 **"Login"**
2. 选择 **"Login with GitHub"**
3. 授权 Railway 访问您的 GitHub 账号
4. 完成注册

**截图说明**:
```
┌─────────────────────────────────┐
│  Railway.app                    │
│                                 │
│  [Login with GitHub]  ← 点击这里 │
│  [Login with Email]             │
└─────────────────────────────────┘
```

---

## 🎯 第二步：推送代码到 GitHub

### 2.1 初始化 Git 仓库（如果还没有）
```bash
# 在项目根目录运行
git init
git add .
git commit -m "Initial commit"
```

### 2.2 创建 GitHub 仓库
1. 访问 https://github.com/new
2. 填写仓库名称：`blueprint-saas`
3. 选择 **Public** 或 **Private**
4. 点击 **"Create repository"**

### 2.3 推送代码
```bash
# 添加远程仓库
git remote add origin https://github.com/你的用户名/blueprint-saas.git

# 推送代码
git branch -M main
git push -u origin main
```

**验证**: 访问您的 GitHub 仓库，确认代码已上传

---

## 🎯 第三步：创建 Railway 项目

### 3.1 创建新项目
1. 登录 Railway
2. 点击 **"New Project"**
3. 选择 **"Deploy from GitHub repo"**

**截图说明**:
```
┌─────────────────────────────────┐
│  New Project                    │
│                                 │
│  ○ Deploy from GitHub repo      │
│  ○ Deploy from template         │
│  ○ Empty project                │
└─────────────────────────────────┘
```

### 3.2 选择仓库
1. 在列表中找到 `blueprint-saas`
2. 点击仓库名称
3. Railway 会自动检测项目类型

**提示**: 如果看不到仓库，点击 **"Configure GitHub App"** 授权更多仓库

---

## 🎯 第四步：添加 PostgreSQL 数据库

### 4.1 添加数据库服务
1. 在项目页面，点击 **"New"**
2. 选择 **"Database"**
3. 选择 **"Add PostgreSQL"**

**截图说明**:
```
┌─────────────────────────────────┐
│  Add Service                    │
│                                 │
│  ○ PostgreSQL  ← 选择这个        │
│  ○ MySQL                        │
│  ○ MongoDB                      │
│  ○ Redis                        │
└─────────────────────────────────┘
```

### 4.2 等待数据库创建
- Railway 会自动创建数据库
- 大约需要 30 秒
- 创建完成后会显示绿色的 ✓

### 4.3 查看数据库连接信息
1. 点击 PostgreSQL 服务
2. 切换到 **"Variables"** 标签
3. 可以看到以下变量：
   - `PGHOST`
   - `PGPORT`
   - `PGDATABASE`
   - `PGUSER`
   - `PGPASSWORD`

**重要**: 这些变量会自动注入到其他服务中

---

## 🎯 第五步：配置后端服务

### 5.1 添加后端服务
1. 点击 **"New"**
2. 选择 **"GitHub Repo"**
3. 选择 `blueprint-saas` 仓库
4. 选择 **"server"** 目录（如果有）

### 5.2 配置环境变量
1. 点击后端服务
2. 切换到 **"Variables"** 标签
3. 点击 **"New Variable"**
4. 添加以下变量：

```bash
# 数据库配置（使用 Railway 的引用语法）
DATABASE_URL=${{Postgres.DATABASE_URL}}

# 或者分别配置
DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_NAME=${{Postgres.PGDATABASE}}
DB_USER=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}

# JWT 密钥（自己生成一个随机字符串）
JWT_SECRET=your-super-secret-jwt-key-change-this

# Node 环境
NODE_ENV=production

# 端口（Railway 会自动设置，但可以明确指定）
PORT=5000

# CORS 配置（前端地址，稍后会更新）
CORS_ORIGIN=https://your-frontend.railway.app
```

**生成 JWT_SECRET**:
```bash
# 在本地运行生成随机字符串
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 5.3 配置构建命令（如果需要）
1. 切换到 **"Settings"** 标签
2. 找到 **"Build Command"**
3. 设置为：`npm install`
4. 找到 **"Start Command"**
5. 设置为：`npm start`

### 5.4 等待部署
- Railway 会自动构建和部署
- 可以在 **"Deployments"** 标签查看进度
- 部署成功后会显示绿色的 ✓

### 5.5 获取后端地址
1. 切换到 **"Settings"** 标签
2. 找到 **"Domains"** 部分
3. 点击 **"Generate Domain"**
4. 会生成类似：`blueprint-backend.railway.app`
5. **记录这个地址**，前端需要用到

---

## 🎯 第六步：配置前端服务

### 6.1 添加前端服务
1. 点击 **"New"**
2. 选择 **"GitHub Repo"**
3. 选择 `blueprint-saas` 仓库
4. 选择根目录

### 6.2 配置环境变量
1. 点击前端服务
2. 切换到 **"Variables"** 标签
3. 添加以下变量：

```bash
# API 地址（使用上一步获取的后端地址）
VITE_API_URL=https://blueprint-backend.railway.app/api

# Gemini API Key（如果需要）
GEMINI_API_KEY=your-gemini-api-key
```

### 6.3 配置构建命令
1. 切换到 **"Settings"** 标签
2. 设置 **"Build Command"**: `npm install && npm run build`
3. 设置 **"Start Command"**: `npx serve -s dist -l 3000`

**或者使用 Nginx**:
```bash
# 如果使用 Dockerfile
# Railway 会自动检测并使用 Docker 构建
```

### 6.4 生成前端域名
1. 切换到 **"Settings"** 标签
2. 找到 **"Domains"** 部分
3. 点击 **"Generate Domain"**
4. 会生成类似：`blueprint-frontend.railway.app`

### 6.5 更新后端 CORS 配置
1. 回到后端服务
2. 更新 `CORS_ORIGIN` 变量
3. 设置为前端域名：`https://blueprint-frontend.railway.app`
4. 后端会自动重新部署

---

## 🎯 第七步：初始化数据库

### 7.1 连接到数据库
有两种方法：

**方法 1: 使用 Railway CLI**
```bash
# 安装 Railway CLI
npm install -g @railway/cli

# 登录
railway login

# 连接到项目
railway link

# 连接到数据库
railway connect postgres
```

**方法 2: 使用本地工具**
```bash
# 获取数据库连接字符串
# 在 Railway 的 PostgreSQL 服务中复制 DATABASE_URL

# 使用 psql 连接
psql "postgresql://user:password@host:port/database"
```

### 7.2 运行数据库迁移
```bash
# 方法 1: 在本地运行迁移
DATABASE_URL="你的数据库URL" npm run migrate

# 方法 2: 在 Railway 中运行
# 在后端服务的 Settings 中添加 "Deploy Command"
# 设置为: npm run migrate && npm start
```

### 7.3 验证数据库
```sql
-- 连接到数据库后运行
\dt  -- 查看所有表
SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='public';
```

---

## 🎯 第八步：测试和验证

### 8.1 测试后端
```bash
# 访问后端健康检查
curl https://blueprint-backend.railway.app/health

# 应该返回
{"status":"ok","timestamp":"2026-01-30T..."}
```

### 8.2 测试前端
1. 访问前端地址：`https://blueprint-frontend.railway.app`
2. 应该能看到登录页面
3. 尝试注册账户
4. 尝试登录

### 8.3 查看日志
1. 在 Railway 项目中点击服务
2. 切换到 **"Deployments"** 标签
3. 点击最新的部署
4. 可以看到实时日志

---

## 🎯 第九步：配置自定义域名（可选）

### 9.1 准备域名
- 如果您有自己的域名（如 `yourdomain.com`）
- 可以将其指向 Railway

### 9.2 添加自定义域名
1. 在服务的 **"Settings"** 中
2. 找到 **"Domains"** 部分
3. 点击 **"Custom Domain"**
4. 输入您的域名：`app.yourdomain.com`
5. Railway 会提供 CNAME 记录

### 9.3 配置 DNS
1. 登录您的域名注册商
2. 添加 CNAME 记录：
   ```
   类型: CNAME
   名称: app
   值: your-app.railway.app
   ```
3. 等待 DNS 生效（5-30 分钟）

### 9.4 启用 HTTPS
- Railway 会自动为自定义域名申请 SSL 证书
- 大约需要 5-10 分钟
- 完成后会显示 🔒 图标

---

## 🎯 第十步：设置自动部署

### 10.1 配置 GitHub 集成
Railway 默认已启用自动部署：
- 每次推送到 `main` 分支
- Railway 会自动重新部署

### 10.2 配置部署分支
1. 在服务的 **"Settings"** 中
2. 找到 **"Source"** 部分
3. 可以更改监听的分支
4. 默认是 `main`

### 10.3 测试自动部署
```bash
# 修改代码
echo "// test" >> server/src/index.ts

# 提交并推送
git add .
git commit -m "Test auto deploy"
git push

# Railway 会自动检测并重新部署
```

---

## 📊 监控和管理

### 查看资源使用情况
1. 在项目页面点击 **"Usage"**
2. 可以看到：
   - CPU 使用率
   - 内存使用率
   - 网络流量
   - 免费额度剩余

### 查看日志
1. 点击服务
2. 切换到 **"Deployments"**
3. 点击部署查看日志

### 重启服务
1. 点击服务
2. 切换到 **"Settings"**
3. 点击 **"Restart"**

---

## 🔧 常见问题

### Q1: 部署失败怎么办？
**A**: 
1. 查看部署日志
2. 检查环境变量是否正确
3. 确认 `package.json` 中的脚本是否正确
4. 检查端口配置（Railway 使用 `PORT` 环境变量）

### Q2: 数据库连接失败？
**A**:
1. 确认数据库服务已启动
2. 检查环境变量引用语法：`${{Postgres.PGHOST}}`
3. 查看后端日志中的错误信息

### Q3: 前端无法连接后端？
**A**:
1. 检查 `VITE_API_URL` 是否正确
2. 确认后端 `CORS_ORIGIN` 包含前端域名
3. 检查后端是否正常运行

### Q4: 超出免费额度怎么办？
**A**:
1. 查看 Usage 页面了解使用情况
2. 可以升级到 $5/月的 Hobby 计划
3. 或者优化应用减少资源使用

### Q5: 如何备份数据？
**A**:
```bash
# 使用 Railway CLI
railway run pg_dump > backup.sql

# 或者使用 psql
pg_dump "postgresql://..." > backup.sql
```

---

## 💡 优化建议

### 1. 使用环境变量引用
```bash
# 好的做法
DATABASE_URL=${{Postgres.DATABASE_URL}}

# 避免硬编码
DATABASE_URL=postgresql://user:pass@host:5432/db
```

### 2. 配置健康检查
在 `server/src/index.ts` 中添加：
```javascript
app.get('/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});
```

### 3. 使用 Railway 的环境变量
```javascript
const PORT = process.env.PORT || 5000;
app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});
```

### 4. 配置日志
```javascript
// 使用 console.log 输出日志
// Railway 会自动收集
console.log('Server started');
console.error('Error occurred');
```

---

## 🎉 完成！

恭喜！您已经成功在 Railway 上部署了应用。

**访问地址**:
- 前端: https://blueprint-frontend.railway.app
- 后端: https://blueprint-backend.railway.app

**下一步**:
1. 测试所有功能
2. 配置自定义域名
3. 设置监控和告警
4. 定期备份数据

---

## 📚 相关资源

- Railway 官方文档: https://docs.railway.app/
- Railway 社区: https://discord.gg/railway
- Railway 博客: https://blog.railway.app/

---

**最后更新**: 2026-01-30  
**难度**: ⭐ 最简单  
**推荐指数**: ⭐⭐⭐⭐⭐
