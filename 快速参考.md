# 快速参考指南

## 🚀 快速启动

### 启动所有服务
```bash
docker-compose up -d
```

### 访问应用
- **前端**: http://localhost:3000
- **后端 API**: http://localhost:5000
- **健康检查**: http://localhost:5000/health

---

## 📋 常用命令

### 查看服务状态
```bash
docker ps
```

### 查看日志
```bash
# 所有服务
docker-compose logs -f

# 特定服务
docker logs -f blueprint_backend
docker logs -f blueprint_frontend
docker logs -f blueprint_postgres
```

### 停止服务
```bash
docker-compose down
```

### 重启服务
```bash
docker-compose restart
```

---

## 🗄️ 数据库操作

### 连接数据库
```bash
docker exec -it blueprint_postgres psql -U postgres -d blueprint_saas
```

### 查看所有表
```bash
docker exec blueprint_postgres psql -U postgres -d blueprint_saas -c "\dt"
```

### 备份数据库
```bash
docker exec blueprint_postgres pg_dump -U postgres blueprint_saas > backup.sql
```

### 恢复数据库
```bash
docker exec -i blueprint_postgres psql -U postgres blueprint_saas < backup.sql
```

---

## 🔧 开发工作流

### 修改后端代码
```bash
# 1. 编辑代码
# 2. 重建镜像
docker-compose build backend

# 3. 重启服务
docker-compose up -d backend

# 4. 查看日志
docker logs -f blueprint_backend
```

### 修改前端代码
```bash
# 1. 编辑代码
# 2. 重建镜像
docker-compose build frontend

# 3. 重启服务
docker-compose up -d frontend

# 4. 查看日志
docker logs -f blueprint_frontend
```

---

## 🧪 API 测试

### 健康检查
```bash
curl http://localhost:5000/health
```

### 获取项目列表（需要认证令牌）
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://localhost:5000/api/projects
```

---

## 📊 系统状态

| 服务 | 地址 | 状态 |
|------|------|------|
| 前端 | http://localhost:3000 | ✅ |
| 后端 | http://localhost:5000 | ✅ |
| 数据库 | localhost:5432 | ✅ |
| Redis | localhost:6379 | ✅ |

---

## 🔐 环境变量

### 后端 (server/.env)
```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blueprint_saas
DB_USER=postgres
DB_PASSWORD=o1L7F%HlA+n*kb4f5j
REDIS_HOST=localhost
REDIS_PORT=6379
JWT_SECRET=UXRMCY4B2NumI9LWtwdlfjDHi03Ks68opnEJxSbc
PORT=5000
NODE_ENV=development
```

### 前端 (.env.local)
```
VITE_API_URL=http://localhost:5000/api
GEMINI_API_KEY=PLACEHOLDER_API_KEY
```

---

## 🆘 故障排查

### 后端无法启动
```bash
# 查看日志
docker logs blueprint_backend

# 检查数据库连接
docker exec blueprint_postgres psql -U postgres -d blueprint_saas -c "SELECT 1"

# 重启
docker restart blueprint_backend
```

### 前端无法加载
```bash
# 查看日志
docker logs blueprint_frontend

# 检查端口
netstat -ano | findstr :3000

# 重启
docker restart blueprint_frontend
```

### 数据库连接失败
```bash
# 查看日志
docker logs blueprint_postgres

# 检查密码
# 编辑 .env 文件中的 DB_PASSWORD

# 重启
docker restart blueprint_postgres
```

---

## 📚 完整文档

- `SYSTEM_STARTUP_COMPLETE.md` - 启动完成报告
- `STARTUP_VERIFICATION_REPORT.md` - 启动验证报告
- `QUICK_STARTUP_COMMANDS.md` - 详细命令
- `server/API_DOCUMENTATION.md` - API 文档
- `server/DEVELOPMENT_GUIDE.md` - 开发指南

---

## 💡 常见问题

**Q: 如何修改数据库密码？**  
A: 编辑 `.env` 文件，然后运行 `docker-compose down && docker-compose up -d`

**Q: 如何查看实时日志？**  
A: 运行 `docker-compose logs -f`

**Q: 如何重置数据库？**  
A: 运行 `docker-compose down -v && docker-compose up -d`

**Q: 如何备份数据？**  
A: 运行 `docker exec blueprint_postgres pg_dump -U postgres blueprint_saas > backup.sql`

---

**最后更新**: 2026-01-29  
**系统状态**: ✅ 全部运行中
